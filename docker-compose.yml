services:
  finance-bot:
    build: .
    image: finance-bot:latest
    container_name: finance-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data/finance
      - ./logs:/app/logs
    networks:
      - bot-network

networks:
  bot-network:
    driver: bridge
